name: Render Mermaid Diagrams
on:
  push:
    paths:
      - 'docs/*.mmd' # Trigger only when mermaid files change

jobs:
  render:
    # Grant the action permission to write to the repository
    permissions:
      contents: write
    uses: fizyk/actions-reuse/.github/workflows/shared-diagram.yml@42b61e053c1d70a61deb42b323c1a2229e2e7fe0 # v5.5.0
